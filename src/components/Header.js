import RelaxLogo from "../assets/images/logo.png";

const Header = () => {
  return (
    <header>
      <div className="wrapper">
        <img src={RelaxLogo} alt="Relax Massage logo" />
      </div>
    </header>
  );
};

export default Header;
